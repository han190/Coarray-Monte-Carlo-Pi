# A Simple Example of Coarray
A simple example that calculates the value of pi using the Monte Carlo method

## Build and run

```
fpm build --compiler caf
fpm run --compiler caf --runner "cafrun -np 4"
```
